# Контекстно-вільні граматики

## Контекст нотатки

- [[Базові поняття]]
%%
- maybe other reference
%%
## Зміст нотатки

%% Insert text %%
### Визначення

>_Контекстно-вільною граматикою_ є четвірка $G=(\Sigma,\mathrm{N},S,\mathcal{P})$ така, що
>   - $\Sigma$ є алфавітом мови, символи якого зазвичай називаються _терміналами_;
>   - $\mathrm{N}$ є алфавітом додаткових символів, що зазвичай називаються _нетерміналами_, одним з яких є символ $S$;
>   - $S$ є початковим _нетерміналом_;
>   - $\mathcal{P}$ є скінченою множиною пар виду $(A,\alpha)$, де $A\in\mathrm{N}$, а $\alpha\in(\Sigma\cup\mathrm{N})^*$. 

Пара $(A,\alpha)\in\mathcal{P}$ зазвичай записується як $A\rightarrow\alpha$.

Пари $A\rightarrow\alpha_1,\ldots,A\rightarrow\alpha_n$ об'єднується у $A$-правило $A\rightarrow\alpha_1\mid\ldots\mid\alpha_n$. В такому правилі $\alpha_1,\ldots,\alpha_n$ називаються _альтернативами_.

### Як граматика породжує мову?

1. Створимо чергу $Q$ і поставимо $S$ в цю чергу.
2. Головний цикл, який завершується, якщо черга $Q$ порожня.
   3. Візьмемо $\alpha\in(\Sigma\cup\mathrm{N})^\ast$ - голову черги $Q$.
   4. Якщо $\alpha\in\Sigma^\ast$, тоді оголосимо $\alpha$ ланцюжком породжуваної мови і перейдемо на початок циклу.
   5. Нехай $\alpha=\beta A\gamma$, де $\beta\in\mathrm{N}^\ast$, $A\in\Sigma$, $\gamma\in(\Sigma\cup\mathrm{N})^\ast$.
   6. Виберемо правило $A\rightarrow\alpha_1\mid\ldots\mid\alpha_n\in\mathcal{P}$.
   7. Поставимо у чергу $Q$ ланцюжки $\beta\alpha_1\gamma,\ldots,\beta\alpha_n\beta$ і перейдемо на початок циклу. 

### Що таке синтаксичне дерево ланцюжка?

Дерево, вершини якого помічені терміналами та нетерміналами, є синтаксичним деревом ланцюжка $u\in\Sigma$, якщо
- корінь дерева помічений нетерміналом $S$;
- листи дерева помічені з ліва направо символами $u$;
- внутрішні вузли, окрім кореня, помічені нетерміналами так, що діти вузла поміченого нетерміналом $A$, помічені з ліва направо символами $\alpha\in(\Sigma\cup\mathrm{N})^\ast$, де $\alpha$ є однією з альтернатив $A$-правила.
