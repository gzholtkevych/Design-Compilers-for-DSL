def split_text(text: str, whitespaces: str, symbols: str) -> list[str]:
    wait_char = 0
    current_state, out_List = wait_char, []
    for ch in text:
        
    pass

# def recognize_keywords(text: str, keywords: list[str])